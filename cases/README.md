# cases

Benchmark mechanisms and expected outputs live here.

Each case should eventually include:
- one topology description input
- expected topology or IR checkpoints
- target poses, bounds, and initial guesses
- expected variable counts, constraint counts, and residual baselines
