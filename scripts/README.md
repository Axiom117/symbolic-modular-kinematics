# scripts

Thin workflow entry points belong here.

Examples:
- validate a case
- export a normalized IR
- run regression batches
- generate debug reports

Keep reusable logic in src/ and use scripts/ only as wrappers.
