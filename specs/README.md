# specs

This directory is the machine-readable source of truth for the project.

Use it to store:
- dsl/: topology encoding syntax, grammar notes, and versioned examples
- schema/: JSON/YAML schema definitions and validation rules
- ir/: normalized topology, chain, and constraint representations
- solver-contracts/: solver inputs, outputs, diagnostics, and error formats
